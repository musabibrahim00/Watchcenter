/**
 * Re-export proxy — canonical source is app/components/Header.tsx
 * Kept here for backwards-compatibility with any remaining relative imports.
 */
export { default } from "../app/components/Header";
